# API URL - în producție folosim proxy-ul local (/api)
# Pentru Vercel: setează NEXT_PUBLIC_API_URL= (gol sau /api)
NEXT_PUBLIC_API_URL=

# Backend URL (doar server-side, pentru API proxy)
BACKEND_URL=https://acont-production.up.railway.app

# JWT settings for middleware (same as backend)
JWT_SECRET=your-jwt-secret-here
JWT_ISSUER=https://acont-production.up.railway.app
